
#### 0.1.1.1 **1. 代码目录结构**
```
sat_iot_terminal/
├── prj/                   // 项目编译
│
└──src/
    ├── crypto             // AES-128加密/MIC生成
    ├── mac                // MAC帧封装/解析
    ├── phy                // 物理层收发驱动
    ├── time               // 终端时钟模块
    ├── app_interface      // 应用数据采集/下发
    ├── phy_interface      // 物理层数据收发接口
    ├── state_machine      // 状态机核心逻辑
    ├── task               // 定时器与线程创建
    ├── VOS                // 跨操作系统接口库
    ├── TsnPlatForm        // 套接字创建与网口配置依赖库
    ├── TsnLogger          // 日志输出
    └── main               // 主程序入口

```

---

#### 0.1.1.2 **2. 状态机设计**

##### 0.1.1.2.1 **2.1 状态定义**
```c
typedef enum {
    STATE_INIT,             // 初始化状态（硬件自检）
    STATE_SEARCH_SYNC,      // 搜星与同步（接收广播）
    STATE_ACTIVATION_REQ,   // 激活请求（发送请求，等待响应）
    STATE_NORMAL_OPERATION, // 正常工作（发送数据，接收下行）
    STATE_SLEEP,            // 休眠（低功耗模式）
    STATE_ERROR_HANDLING,   // 异常处理（重传/恢复）
    STATE_REACTIVATION      // 重激活（密钥更新/配置变更）
} TerminalState;
```

##### 0.1.1.2.2 **2.2 状态转换图**
```
[初始化] → (成功) → [搜星与同步]
[搜星与同步] → (收到广播) → [激活请求]
[激活请求] → (收到激活响应) → [正常工作]
[正常工作] → (定时器到期) → [休眠]
[正常工作] → (发送数据) → [休眠]
[休眠] → (唤醒事件) → [正常工作]
[任何状态] → (异常触发) → [异常处理]
[异常处理] → (恢复成功) → [搜星与同步]
[正常工作] → (重激活命令) → [重激活]
[重激活] → (完成) → [正常工作]
```

##### 0.1.1.2.3 **2.3 状态转换条件**
| 当前状态            | 转换条件                          | 下一状态           |
|---------------------|-----------------------------------|--------------------|
| `STATE_INIT`        | 硬件初始化完成                    | `STATE_SEARCH_SYNC`|
| `STATE_SEARCH_SYNC` | 成功接收广播消息                  | `STATE_ACTIVATION_REQ`|
| `STATE_ACTIVATION_REQ` | 收到激活响应（ACK）           | `STATE_NORMAL_OPERATION`|
| `STATE_NORMAL_OPERATION` | 发送数据完成/接收窗关闭      | `STATE_SLEEP`      |
| `STATE_SLEEP`       | 定时器唤醒/应用数据触发发送       | `STATE_NORMAL_OPERATION`|
| 任意状态            | 连续3次未收到ACK/超时            | `STATE_ERROR_HANDLING`|
| `STATE_ERROR_HANDLING` | 重传成功/恢复配置              | `STATE_SEARCH_SYNC`|
| `STATE_NORMAL_OPERATION` | 收到重激活命令（0x4）       | `STATE_REACTIVATION`|
| `STATE_REACTIVATION` | 完成密钥更新/配置更新             | `STATE_NORMAL_OPERATION`|

---

#### 0.1.1.3 **3. 核心模块设计**

##### 0.1.1.3.1 **3.1 加密与安全模块 (`crypto.c`)**
- **功能**：
  - 使用AES-128加密应用数据。
  - 生成消息完整性校验码（MIC）。
  - 存储根密钥（NetKey、AppKey），无外部读取接口。
- **接口**：
  ```c
  void aes128_encrypt(uint8_t *plaintext, uint8_t *key, uint8_t *ciphertext);
  void generate_mic(uint8_t *data, uint32_t len, uint8_t *key, uint8_t *mic);
  ```

##### 0.1.1.3.2 **3.2 MAC层模块 (`mac.c`)**
- **功能**：
  - 组帧：封装IoT MAC帧（消息类型、空口标识、MIC）。
  - 解析：解析下行NS MAC帧（完整标识、MIC校验）。
  - 处理切片业务（首包携带空口标识，后续包仅序号）。
- **帧格式**：
  ```c
  typedef struct {
      uint8_t  msg_type;      // 4 bits（消息类型）
      uint16_t air_id;        // 16 bits（空口标识/组标识）
      uint8_t  mic[12];       // 12 bits MIC
      uint8_t  payload[72];   // 业务数据（最大72 bits）
  } IoTMacFrame;
  ```

##### 0.1.1.3.3 **3.3 PHY层模块 (`phy.c`)**
- **功能**：
  - 物理帧组帧：添加前导码、CRC校验。
  - 根据终端能力选择数据速率（支持3种速率配置）。
  - 驱动射频模块发送/接收数据。
- **接口**：
  ```c
  void phy_send(uint8_t *data, uint32_t len, uint8_t rate);
  bool phy_receive(uint8_t *buffer, uint32_t timeout);
  ```

##### 0.1.1.3.4 **3.4 定时与终端控制模块 (`timer_ctrl.c`)**
- **功能**：
  - 管理接收窗时序（B类终端周期性开启，A类终端发送后开启）。
  - 校准本地时钟（基于广播消息的时间基准）。
  - 控制低功耗模式（休眠/唤醒）。
- **关键逻辑**：
  ```c
  void configure_reception_window(uint8_t window_length, uint16_t frequency) {
      // 配置接收窗参数（长度、频点、周期）
      // 触发硬件定时器中断
  }
  ```

##### 0.1.1.3.5 **3.5 应用接口模块 (`app_interface.c`)**
- **功能**：
  - 从传感器采集应用数据（如温度、位置）。
  - 将解析后的下行数据传递给用户应用。
- **接口**：
  ```c
  void app_data_collect(uint8_t *buffer);
  void app_data_deliver(uint8_t *data);
  ```

---

#### 0.1.1.4 **4. 关键流程代码示例**

##### 0.1.1.4.1 **4.1 激活请求流程**
```c
// state_machine.c
void handle_activation_request() {
    IoTMacFrame frame;
    frame.msg_type = 0x2; // 激活请求
    frame.air_id = get_air_id();
    frame.payload = pack_activation_payload(); // 包含物理标识、DevNonce等
    generate_mic(frame.data, sizeof(frame), NetKey, frame.mic);
    phy_send(frame.data, sizeof(frame), DEFAULT_RATE);
    start_ack_timeout(3000); // 等待3秒
}
```

##### 0.1.1.4.2 **4.2 B类终端接收窗控制**
```c
// timer_ctrl.c
void handle_b_class_window() {
    if (is_broadcast_time()) {
        enable_rx_window(BROADCAST_FREQ, 8); // 8ms接收窗
    } else if (is_unicast_time()) {
        enable_rx_window(UNICAST_FREQ, 4);  // 4ms接收窗
    }
}
```

---

#### 0.1.1.5 **5. 异常处理机制**
- **重传逻辑**：
  - 上行待确认消息（0x4）未收到ACK时，随机退避后重传（最大3次）。
  - 超过重试阈值后触发异常状态，重置协议栈。
- **时钟校准失败**：
  - 逐步增加接收窗长度（2ms → 4ms → 8ms），直至同步成功。


---

#### 0.1.1.6 **6. 测试用例**
1. **激活流程测试**：验证终端从初始化到正常工作的状态转换。
2. **MIC校验测试**：模拟篡改数据，验证完整性检查。
3. **接收窗时序测试**：测量B类终端接收窗的精确性。
4. **异常恢复测试**：强制丢包，验证重传与状态恢复。
